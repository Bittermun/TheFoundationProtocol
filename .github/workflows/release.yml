name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 3.1.2)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ".[test,fuzz]"
          pip install build ruff mypy pytest-cov prometheus_client

      - name: Run linting
        working-directory: tfp-foundation-protocol
        run: |
          ruff check .
          mypy tfp_demo/ tfp_client/ --ignore-missing-imports --no-error-summary

      - name: Run tests
        env:
          TFP_DB_PATH: ":memory:"
        run: |
          python -m pytest -q --tb=short --cov=tfp_demo --cov=tfp_client --cov-report=xml
          python demo_30sec.py --json

      - name: Build package
        run: |
          python -m build
          python scripts/check_distribution.py dist/*.whl

      - name: Generate checksums
        run: |
          cd dist
          sha256sum * > checksums.txt
          cat checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.whl
            dist/*.tar.gz
            dist/checksums.txt
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (manual run)
        uses: actions/upload-artifact@v4
        if: github.event_name == 'workflow_dispatch'
        with:
          name: release-artifacts-${{ inputs.version }}
          path: |
            dist/*.whl
            dist/*.tar.gz
            dist/checksums.txt
